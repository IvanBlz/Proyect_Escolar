<div class="col-lg-10">
    <div id="project">
        <div class="row">
            <div class="mx-auto">
                <h1><em>USER</em></h1>
            </div>
        </div>
        <div class="col-lg-12">
            <!-- Button trigger modal -->
            <a href="signin" class="btn btn-success float-right">+</a>
 <br><br>       </div>
        <div class="col-lg-12">
           <div class="row">
            {{#each Users}}
            <div class="col-lg-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{Nick}}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{{User_Name}}</h6>
                    <h6 class="card-subtitle mb-2 text-muted">{{Email}}</h6>
                    
                </div>
            </div>
            </div>

            {{/each}}
            </div>
        </div>

    </div>
</div>